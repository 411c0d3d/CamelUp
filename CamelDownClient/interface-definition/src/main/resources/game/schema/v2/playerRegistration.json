{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "playerRegistration",
  "title": "playerRegistration",
  "description": "This packet is sent to the server when a player wants to register as a game participant. This can happen as long as the client is not in any lobby. It can also happen when switching from an observer to a participant.",
  "type": "object",
  "properties": {
    "playerName": {
      "type": "string",
      "description": "The name of the player."
      "minLength": 1,
      "maxLength": 20
    }
  },
  "required": [
    "playerName"
  ],
  "additionalProperties": false,
  "examples": [
    {
      "clientName": "Camel"
    }
  ]
}